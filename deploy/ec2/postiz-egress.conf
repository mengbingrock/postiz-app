# Install as a systemd drop-in for both postiz-backend and
# postiz-orchestrator when ChineseInLA should use postiz-egress.
[Service]
Environment="POSTIZ_EGRESS_PROXY_PORT=18443"
Environment="CHINESEINLA_PROXY=http://127.0.0.1:18443"
